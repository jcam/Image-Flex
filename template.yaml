AWSTemplateFormatVersion: 2010-09-09
Transform: AWS::Serverless-2016-10-31

Resources:
  EdgeLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          Effect: Allow
          Principal:
            Service:
            - lambda.amazonaws.com
            - edgelambda.amazonaws.com
          Action:
          - sts:AssumeRole
      Path: /service-role/
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
        - arn:aws:iam::aws:policy/AWSXrayFullAccess
      Policies:
        -
          PolicyName: resizer-images-Lambda-S3
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              Effect: Allow
              Action:
                - s3:*
              Resource:
                - arn:aws:s3:::resizer-images
                - arn:aws:s3:::resizer-images/*

  OriginAccessId:
    Type: AWS::CloudFront::CloudFrontOriginAccessIdentity
    Properties:
      CloudFrontOriginAccessIdentityConfig:
        Comment: resizer-OriginAccessId

  ImageBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: resizer-images
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      CorsConfiguration:
        CorsRules:
          - AllowedOrigins:
              - "*"
            AllowedMethods:
              - "GET"
            AllowedHeaders:
              - "*"

  ImageBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref ImageBucket
      PolicyDocument:
        Id:  resizer-images-policy
        Version: 2012-10-17
        Statement:
          -
            Action:
              - s3:GetObject
            Effect: Allow
            Resource: arn:aws:s3:::resizer-images/*
            Principal:
              AWS: !Sub arn:aws:iam::cloudfront:user/CloudFront Origin Access Identity ${OriginAccessId}

  CloudFrontLoggingBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: resizer-cflogs
      PublicAccessBlockConfiguration:
        BlockPublicAcls: Yes
        BlockPublicPolicy: Yes
        IgnorePublicAcls: Yes
        RestrictPublicBuckets: Yes

  UriToS3KeyFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: resizer-UriToS3Key
      AutoPublishAlias: live
      CodeUri: src/UriToS3Key/
      Handler: index.handler
      Runtime: nodejs12.x
      MemorySize: 128
      Timeout: 1
      Role: !GetAtt EdgeLambdaRole.Arn

  GetOrCreateImageFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: resizer-GetOrCreateImage
      AutoPublishAlias: live
      CodeUri: src/GetOrCreateImage/
      Handler: index.handler
      Runtime: nodejs12.x
      MemorySize: 256
      Timeout: 30
      Role: !GetAtt EdgeLambdaRole.Arn

  WebACL:
    Type: AWS::WAFv2::WebACL
    Properties:
      DefaultAction:
        Allow: {}
      Name: resizer-WebAcl
      Rules:
        - Name: AWS-AWSManagedRulesCommonRuleSet
          Priority: 0
          OverrideAction:
            None: {}
          VisibilityConfig:
            SampledRequestsEnabled: true
            CloudWatchMetricsEnabled: true
            MetricName: resizer-MetricForAMRCRS
          Statement:
            ManagedRuleGroupStatement:
              VendorName: AWS
              Name: AWSManagedRulesCommonRuleSet
              ExcludedRules:
                - Name: NoUserAgent_HEADER
      Scope: CLOUDFRONT
      VisibilityConfig:
        SampledRequestsEnabled: true
        CloudWatchMetricsEnabled: true
        MetricName: resizer-WebAcl

  Distribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        # Uncomment the next two lines to use a custom CNAME (must be configured in Route 53, or your DNS provider).
        # Aliases:
        #   - YOUR CNAME HERE

        ViewerCertificate:
          CloudFrontDefaultCertificate: true
          # Replace the above line with the following to indicate your own certificate to use for HTTPS
          # CloudFrontDefaultCertificate: false
          # AcmCertificateArn: YOUR CERTIFICATE MANAGER ARN HERE
          # SslSupportMethod: sni-only

        Comment: Distribution for resizer
        DefaultCacheBehavior:
          Compress: true
          ForwardedValues:
            QueryString: true
            QueryStringCacheKeys:
              - w
              - h
            Cookies:
              Forward: none
            Headers:
              - Access-Control-Request-Method
              - Access-Control-Request-Headers
              - Origin
          LambdaFunctionAssociations:
            - EventType: viewer-request
              LambdaFunctionARN: UriToS3KeyFunction.Version
            - EventType: origin-response
              LambdaFunctionARN: GetOrCreateImageFunction.Version
          MinTTL: 100
          TargetOriginId: resizer-images
          ViewerProtocolPolicy: allow-all
        Enabled: true
        Logging:
          Bucket: !GetAtt CloudFrontLoggingBucket.DomainName

        Origins:
          - DomainName: resizer-images.s3.amazonaws.com
            Id: resizer-images
            S3OriginConfig:
              OriginAccessIdentity: !Sub origin-access-identity/cloudfront/${OriginAccessId}
        PriceClass: PriceClass_All
        WebACLId: !GetAtt WebACL.Arn

Outputs:
  DistributionDomain:
    Value: !GetAtt Distribution.DomainName
    Description: Cloudfront distribution domain.

  UriToS3KeyFunction:
    Value: !Ref UriToS3KeyFunction
    Description: Lambda function for the Cloudfront viewer-request event.

  GetOrCreateImageFunction:
    Value: !Ref GetOrCreateImageFunction
    Description: Lambda function for the Cloudfront origin-response event.
